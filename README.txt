This site was build to be run on an Apache server running PHP 5.5 and MySQL. Generally, none of the pages on the disk will work properly at all unless you are running a local apache server with PHP 5.5 and MySQL with the root folder set to be the product folder.

Please look at the online version of the site instead (although note that that site is on a rather slow free host).